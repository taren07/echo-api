version: "3.8"
services:
  dev-postgres:
    image: postgres:15.1-alpine
    ports:
      - 5434:5432
    environment:
      POSTGRES_USER: echo-api
      POSTGRES_PASSWORD: echo-api
      POSTGRES_DB: echo-api
    restart: always
    networks:
      - test-api
networks:
  test-api:
